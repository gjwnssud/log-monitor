server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/promtail-positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: logs
    static_configs:
# __SERVERS__ (setup.sh가 이 아래에 서버 항목을 자동 생성합니다)
