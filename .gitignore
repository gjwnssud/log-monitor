# 환경 설정 (개인 정보 포함)
.env
servers.conf
config/grafana/provisioning/alerting/contact-points.yml

# setup.sh가 생성하는 파일
config/alloy/alloy-config.alloy
start-alloy.sh
start-alloy.bat

# ~/.gitignore_global에 docker가 있는 경우 이 프로젝트에서 제외
!docker/

# 데이터 볼륨
data/
